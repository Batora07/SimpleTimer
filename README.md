A simple timer made in react as a recreational purpose as well as discovering the framework 

You need to install yarn, 
then install uuid from yarn with the command 
--> yarn add uuid, 
also need to install prop-types 
(from yarn as well) 